# Archive

The archive preserves the history of the design system without competing with the current source of truth.

- `versions/` contains historical design-system markdown files.
- `experiments/` contains explorations, prototypes, and alternate directions that are not the maintained system.
