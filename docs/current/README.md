# Current design system

`v25/` is the current maintained Stadli design-system specification, split into smaller topic files for easier maintenance.

Start with [`v25/README.md`](v25/README.md). The legacy [`v25.md`](v25.md) file is now only a compatibility pointer so older links do not break.

Keep only the active, maintained system in this folder. Historical versions belong in `../archive/versions/`.
