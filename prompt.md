I've attached a screenshot of a UI design and our design system file (design.md). Recreate the interface in the screenshot as a single, production-quality component — but do NOT copy its visual styling. The screenshot defines WHAT to build (layout structure, content hierarchy, sections, components, interactions implied). Our design.md defines HOW it must look (colors, typography, spacing, radii, shadows, buttons, forms, icons, states, and any markup/framework conventions). Rules:

1. design.md is the single source of truth for all visual decisions. If the screenshot conflicts with design.md, design.md wins — every time.
2. Never invent tokens. Only use colors, font sizes, spacing values, and component patterns defined in design.md. If something in the screenshot has no equivalent in our system, map it to the closest existing pattern and note the substitution.
3. Match the screenshot's structure faithfully: same sections, same content order, same general layout and density. Replace all real text/data from the screenshot with realistic placeholder content of similar length.
4. Build it responsive and include all relevant states defined in our system (hover, focus, disabled, loading, empty) where they apply.
5. Use the stack/conventions specified in design.md. If none are specified, ask before assuming. Before writing code, briefly list: (a) the components you identified in the screenshot, (b) which design.md pattern each one maps to, and (c) any gaps where our system has no equivalent. Then produce the full code in one file. Plain HTML + CSS (single .html)
